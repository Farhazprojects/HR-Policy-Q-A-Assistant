# Frontend

React + TypeScript chat interface where employees ask questions and view cited answers.

| Folder | Purpose |
|---|---|
| `src/` | Application source (components, pages, API client) |
| `public/` | Static assets |

Scaffolding lands in Assessment 2 per `ROADMAP.md`.
