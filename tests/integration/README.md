# Integration Tests
Tests that exercise multiple components together (e.g. API → RAG pipeline → vector store) without mocking the boundaries between them. Added once the backend and AI layer are both scaffolded.
